server:
  port: 8080

spring:
  datasource:
    # Supabase PostgreSQL (默认)
    url: jdbc:postgresql://db.qirlzzpmasmhkbezacro.supabase.co:5432/postgres?sslmode=require
    username: postgres
    password: 3CEp7gUHMNLss30b
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
    # 本地 MySQL (备选，取消注释切换)
    # url: jdbc:mysql://localhost:3306/roco_dex?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    # username: root
    # password: root
    # driver-class-name: com.mysql.cj.jdbc.Driver
  cache:
    type: caffeine
    caffeine:
      spec: maximumSize=1000,expireAfterWrite=30m

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: roco-dex-secret-key-must-be-at-least-256-bits-long-for-hs256
  expiration: 86400000

springdoc:
  swagger-ui:
    path: /doc.html
  api-docs:
    path: /v3/api-docs

knife4j:
  enable: true
  setting:
    language: zh_cn
